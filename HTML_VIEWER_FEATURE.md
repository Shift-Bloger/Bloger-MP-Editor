# HTML 代码查看和编辑功能说明

## 功能概述

在 Vue3 Tiptap 编辑器中添加了 **HTML 代码查看和编辑**功能，用户可以：

1. **查看 HTML 代码** - 点击工具栏中的 "HTML 代码" 按钮，以树形结构展示当前编辑的 HTML
2. **格式化 HTML** - 自动格式化 HTML 代码，提高可读性
3. **展开/合并代码标签** - 像 IDE 编辑器一样，支持代码折叠功能
4. **编辑 HTML** - 切换到编辑模式，直接修改 HTML 代码
5. **实时同步** - 修改的 HTML 代码会实时更新到编辑器中
6. **深色模式适配** - 完美支持深色和浅色模式
7. **代码高亮** - 采用彩色编码展示不同的 HTML 元素和属性

## 组件结构

### 1. `html-viewer.vue` - 主要的 HTML 查看器组件
- 提供模态对话框界面
- 支持预览和编辑两种模式
- 包含工具栏：全部展开/合并、复制、格式化
- 深色模式自适应

**主要功能：**
- `formatHtmlString()` - 格式化 HTML 代码
- `parseHtml()` - 解析 HTML 为树形结构
- `handleSave()` - 保存编辑的 HTML 到编辑器
- `copyToClipboard()` - 复制 HTML 到剪贴板

### 2. `html-tree-node.vue` - HTML 树节点组件
- 递归渲染 HTML 树
- 支持展开/合并功能
- 彩色代码高亮
- 响应式深色模式

**展示内容：**
- 标签名 (如 `<div>`, `<p>`)
- 属性 (如 `class="container"`, `id="main"`)
- 文本内容
- 嵌套关系

### 3. 工具栏集成
在 `toolbar/classic/index.vue` 中添加了新的工具按钮
在 `toolbar/classic/classic-options.ts` 中定义了 HTML 查看器选项

## 使用方法

### 打开 HTML 查看器

1. 点击工具栏中的 "HTML 代码" 按钮（图标：`ri-code-line`）
2. 弹出模态对话框，显示当前编辑内容的 HTML

### 预览模式（默认）

- 以树形结构展示 HTML
- 鼠标悬停在标签上会高亮显示
- 点击 `▶` 图标可展开/合并子标签
- 使用 "全部展开"/"全部合并" 按钮快速切换

### 编辑模式

1. 点击右上角的开关切换到编辑模式
2. 在文本区域编辑 HTML 代码
3. 点击 "格式化" 按钮自动格式化代码
4. 点击 "保存" 按钮提交更改

### 其他功能

- **复制** - 将 HTML 代码复制到剪贴板
- **格式化** - 美化 HTML 代码结构
- **展开/合并** - 控制代码树的展开状态

## 样式和主题

### 浅色模式
- 标签：紫色 (`#881280`)
- 属性名：橙色 (`#ff8c42`)
- 属性值：绿色 (`#28a745`)
- 背景：浅灰 (`#f5f5f5`)

### 深色模式
- 标签：浅紫 (`#c472ff`)
- 属性名：橙色 (`#ffa500`)
- 属性值：浅绿 (`#67d88c`)
- 背景：深灰 (`#0d1117`)

## 技术细节

### HTML 格式化算法
- 识别自闭合标签（`<br/>`, `<img/>` 等）
- 正确处理缩进
- 保留文本内容的格式
- 支持注释和特殊标签

### 树形解析
- 使用浏览器 DOM API 解析 HTML
- 递归构建树形结构
- 保留标签属性信息
- 识别文本节点

### 深色模式检测
- 监听 `document.body` 的 `dark-mode` class
- 动态更新样式变量
- 支持实时切换

## 文件列表

```
src/components/vue3-tiptap/toolbar/classic/
├── html-viewer.vue          # 主要 HTML 查看器组件
├── html-tree-node.vue       # HTML 树节点组件
├── index.vue                # 工具栏（已更新）
└── classic-options.ts       # 工具栏配置（已更新）
```

## 注意事项

1. **不影响现有功能** - 新功能完全独立，不修改编辑器核心
2. **大型 HTML** - 如果 HTML 代码过大，可能会影响渲染性能
3. **浏览器兼容性** - 需要支持 ES6 和 DOM API 的浏览器
4. **错误处理** - 无效的 HTML 会显示警告信息

## 示例

### 编辑前的 HTML
```html
<div class="content"><p style="color: red;">Hello World</p><span>Test</span></div>
```

### 树形展示
```
<div class="content">
  <p style="color: red;">
    Hello World
  </p>
  <span>
    Test
  </span>
</div>
```

## 未来改进方向

1. 添加语法检查
2. 支持更多的快捷键
3. 添加 HTML 验证
4. 支持自定义样式规则
5. 添加代码搜索功能
6. 支持实时预览更新
