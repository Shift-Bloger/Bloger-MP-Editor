# HTML 代码查看器功能测试说明

## 测试场景

### 场景 1：基本的 HTML 预览
1. 打开应用程序
2. 在编辑器中输入一些内容（如标题、段落、加粗文本等）
3. 点击工具栏中的"HTML 代码"按钮（代码图标）
4. 应该看到格式化的 HTML 树形结构
5. 点击展开/合并按钮来展开或隐藏 HTML 标签

**预期结果：**
- 弹出一个大的模态对话框
- 显示当前编辑内容的 HTML 结构
- 可以清晰地看到标签、属性和文本内容

### 场景 2：编辑 HTML 代码
1. 在 HTML 查看器中点击切换开关
2. 切换到编辑模式（应该看到"Edit"文本）
3. 在文本区域中编辑 HTML 代码
4. 点击"保存"按钮
5. 检查编辑器中的内容是否已更新

**预期结果：**
- 成功切换到编辑模式
- 能够编辑 HTML 代码
- 保存后，编辑器中的内容会实时更新
- 出现成功消息

### 场景 3：格式化 HTML
1. 在编辑模式下，输入未格式化的 HTML（例如：`<div><p>test</p></div>`）
2. 点击"格式化"按钮
3. HTML 代码应该被自动缩进和格式化

**预期结果：**
- HTML 代码被正确格式化
- 标签正确缩进
- 代码更易读

### 场景 4：展开/合并
1. 在预览模式下，点击"全部展开"按钮
2. 所有 HTML 标签都应该展开
3. 点击"全部合并"按钮
4. 所有嵌套标签都应该折叠

**预期结果：**
- 按钮文本动态改变
- 所有可折叠的节点状态改变
- 可以点击单个标签旁的三角形来个别展开/合并

### 场景 5：复制功能
1. 在任何模式下，点击"复制"按钮
2. 应该看到"已复制到剪贴板"消息
3. 打开文本编辑器，粘贴内容

**预期结果：**
- 显示成功的提示消息
- HTML 代码被成功复制到剪贴板
- 可以粘贴到其他应用程序

### 场景 6：深色模式
1. 点击页面头部的主题切换按钮（太阳/月亮图标）
2. 页面变为深色模式
3. 打开 HTML 查看器
4. 检查颜色是否适应深色模式

**预期结果：**
- HTML 查看器使用深色主题的颜色
- 代码仍然易于阅读
- 没有颜色冲突

### 场景 7：复杂 HTML 结构
1. 创建一个包含多个嵌套元素的内容：
   - 表格
   - 列表（有序和无序）
   - 块引用
   - 代码块
   - 内联样式
2. 打开 HTML 查看器
3. 检查树形结构是否正确展示所有元素

**预期结果：**
- 所有元素都正确解析
- 嵌套关系清晰
- 属性（class、id、style 等）正确显示

## 代码示例

### 测试用例 1：简单段落

```html
<p>这是一个简单的段落。</p>
```

预期 HTML 树：
```
<p>
  这是一个简单的段落。
</p>
```

### 测试用例 2：格式化文本

```html
<p>这是<strong>粗体</strong>和<em>斜体</em>文本。</p>
```

预期 HTML 树：
```
<p>
  这是
  <strong>
    粗体
  </strong>
  和
  <em>
    斜体
  </em>
  文本。
</p>
```

### 测试用例 3：带属性的元素

```html
<div class="container" id="main" style="color: red;">
  <p>内容</p>
</div>
```

预期 HTML 树：
```
<div class="container" id="main" style="color: red;">
  <p>
    内容
  </p>
</div>
```

## 性能测试

1. **大型 HTML** - 测试包含超过 1000 个元素的 HTML
2. **嵌套深度** - 测试 10+ 层深的嵌套结构
3. **特殊字符** - 测试包含特殊字符和 HTML 实体的内容

## 错误处理测试

1. **无效 HTML** - 输入格式错误的 HTML 代码
2. **缺少闭标签** - 输入未闭合的标签
3. **脚本标签** - 测试是否正确处理 `<script>` 标签

## 验收标准

- [ ] HTML 查看器按钮出现在工具栏中
- [ ] 点击按钮打开模态对话框
- [ ] 预览模式正确显示 HTML 树
- [ ] 展开/合并功能正常工作
- [ ] 编辑模式允许修改 HTML
- [ ] 保存后编辑器内容更新
- [ ] 复制功能有效
- [ ] 深色模式下颜色正确
- [ ] 代码高亮清晰
- [ ] 不破坏现有功能
- [ ] 性能可接受（<1 秒打开）
